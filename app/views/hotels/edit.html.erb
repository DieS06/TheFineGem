<section class="sec-edit-hotels">
    <div class="main-title">
        <h2>Update <%= @hotel.name %> Hotel</h2>
    </div>

    <div class="form-container">
        <% if flash[:alert] %>
            <div class="alert alert-danger">
                <%= flash[:alert] %>
            </div>
        <% end %>

        <%= render 'form_edit', hotel: @hotel %>
    </div>
</section>